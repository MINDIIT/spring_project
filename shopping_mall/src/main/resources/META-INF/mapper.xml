<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Shopping_mall">
	<select id="search" parameterType="Map" resultType="userdao">
		select * from user
		<choose><!-- 여러가지 형태의 쿼리문을 사용시 적용하는 태그 where+if-->
			<when test="part==1"><!-- 아이디 찾기 -->
				where uname=#{uname} and uemail=#{uemail}
			</when>
			<when test="part==2"><!-- 패스워드 찾기 -->
				where uid=#{uid} and uname=#{uname}
			</when>
			<otherwise>
				order by uidx desc;
			</otherwise>
		</choose>
	</select>
	<insert id="admin_standbyadd">
		insert into confirm_standby (aidx,admin_id,admin_pass,admin_name,admin_email,admin_hp,admin_team,admin_position,admin_join)
		values ('0',#{aidx},#{admin_id},#{admin_pass},#{admin_name},#{admin_email},#{admin_hp},#{admin_team},#{admin_position},now())
	</insert>
</mapper>